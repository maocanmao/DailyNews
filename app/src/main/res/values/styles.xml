<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="DarkTheme" parent="AppTheme.NoActionBar">
        <item name="navigation_bg_color">@color/color_deep_gray</item>
        <item name="container_background_color">@color/color_deep_gray</item>
        <item name="text_color">@color/color_text_white</item>
        <item name="navigation_view_title_color">@color/color_text_white</item>
    </style>

    <style name="LightTheme" parent="AppTheme.NoActionBar">
        <item name="navigation_bg_color">@color/colorPrimary</item>
        <item name="container_background_color">@android:color/white</item>
        <item name="text_color">@android:color/black</item>
        <item name="navigation_view_title_color">@android:color/white</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

</resources>
